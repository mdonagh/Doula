<%= form_with(model: registry_type, local: true) do |form| %>
  <% if registry_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registry_type.errors.count, "error") %> prohibited this registry_type from being saved:</h2>

      <ul>
      <% registry_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
